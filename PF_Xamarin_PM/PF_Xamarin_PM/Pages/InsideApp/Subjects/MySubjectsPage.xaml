<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PF_Xamarin_PM.MySubjectsPage">

    <ContentPage.ToolbarItems>
        <ToolbarItem Icon="ic_custom_agregar.png" Clicked="CreateNewSubject"></ToolbarItem>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout Orientation="Vertical" Padding="15">
            <Label HorizontalOptions="CenterAndExpand" FontSize="Large" TextColor="Red"></Label>
            <ListView x:Name="listviewMySubjects" ItemTapped="ShowSubjectInfo"
                      IsPullToRefreshEnabled="True"
                      RefreshCommand="{Binding RefreshCommand}"
                      IsRefreshing="{Binding IsRefreshing}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                                <Image Source="ic_custom_asignaturas.png" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"/>
                                <Label Text="{Binding Name}" FontSize="Large" HorizontalOptions="StartAndExpand"
                                       TextColor="Red" VerticalTextAlignment="Center"></Label>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>